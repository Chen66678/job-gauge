/**
 * 官方输出面构建期总开关（fail-closed 安全默认，不是 gate 判断逻辑）。
 *
 * 依据：D008 §8 + output-faithfulness-gate-criteria.md §4/§8 ——
 * 输出忠实性闸门「形式规范+标定数值」交付物过双审前，官方绝不吐我方生成/导出的
 * 对外内容；发布能力必须是机器可验硬合取，构建期强制关闭。
 *
 * 这是一个**构建期常量**（字面 `false`，非 env / 非运行时配置），使打包器可在
 * 编译期判定分支恒假、做死代码消除。**只可上调流程解锁（改 true）须走：闸门交付物
 * 过挑战者+交叉审计 → 工程实现 → §5 验收 → 发布硬合取签名齐。** 任何人不得为
 * 「先跑通链」把它悄悄改 true —— 有回归测试锁定默认关闭。
 */
export const OUTPUT_GATE_RELEASED = false as const;
