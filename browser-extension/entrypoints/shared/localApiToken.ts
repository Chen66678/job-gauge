// Shared constant so options.ts (writer) and background.ts (reader) agree on
// the same chrome.storage.local key without duplicating the string literal.
export const LOCAL_API_TOKEN_STORAGE_KEY = 'localApiToken';
